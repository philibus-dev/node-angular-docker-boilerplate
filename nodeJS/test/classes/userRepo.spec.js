const expect = require('chai').expect;

const UserRepo = require("../../classes/userRepo");

describe('Test UserRepo Class', () => {

    it('should create', () => {
        // expect(new UserRepo()).not.equal(null);
    });

});